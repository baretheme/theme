{
  "name": "@baretheme/test-utils",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "author": "Marc Mintel <marc@mintel.me> (@mmintel)",
  "license": "MIT",
  "scripts": {
    "dev": "tsc -w",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@types/faker": "^4.1.11"
  }
}
